package com.example.munchkin.util

class Util {
    companion object {
        // Constants for player attributes
        const val DEFAULT_STRENGTH = 1
        const val DEFAULT_LEVEL    = 1
        const val DEFAULT_GEAR     = 0
        const val DEFAULT_MODIFIER = 0

        const val MAX_PLAYERS = 10
    }
}
